Subproblem:
Let L[i] = min penalty to reach ai s.t. 0 <= i <= n

Base case:
L[0] = 0

Recurrence:
L[i] = min_j([200 - ai + aj]^2 + L[j]) for 0 <= j < i <= n

Pseudocode:
L[0] = 0
for i = 1 to n:
  L[i] = infinity
  for j = 0 to i-1:
    x = ai - aj
    penalty = (200 - x) ^ 2
    L[i] = min(penalty + L[j], L(i))
return L(n)

Time complexity:
O(n^2)
