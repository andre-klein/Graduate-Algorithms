# recurrence Let L[i] = min penalty to reach ai s.t. 0 <= i <= n
# Base case: L[0] = 0
# subproblem: L[i] = minj([200 - ai + aj]^2 + L[j]) for 0 <= j < i <= n
# pseudocode:
# L[0] = 0
# for i = 1 to n:
#   L[i] = infinity
#   for j = 0 to i-1:
#     x = ai - aj
#     penalty = (200 - x) ^ 2
#     L[i] = min(penalty + L[j], L(i))
# return L(n)
# Time complexity is O(n^2)
